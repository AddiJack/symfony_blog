{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <strong>Tags : {{ tag }}</strong>
    <ul>
        {% for tag in article %}
            <li>{{ loop.index }} / {{tag.title}} </li>
        {% endfor %}
    </ul>
{% endblock %}